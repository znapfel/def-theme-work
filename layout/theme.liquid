<!DOCTYPE html>
<html>
  <head>
    {{ content_for_header }}
    {{- 'theme.scss.css' | asset_url | stylesheet_tag -}}
  </head>
  <body>
    <header>
      {%- section 'header' -%}
    </header>
    {{ content_for_layout }}
    {{- 'theme.js' | asset_url | script_tag -}}
  </body>
</html>
